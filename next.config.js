/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pravatar.cc', 'utfs.io', '9xm64gpr09.ufs.sh', 'lh3.googleusercontent.com'],
    },
};

module.exports = nextConfig;
